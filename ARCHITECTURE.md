# 대시보드 아키텍처

## 1. 전체 구조

대시보드는 별도의 백엔드 없이 동작하는 정적 단일 페이지 애플리케이션입니다. 생성 단계에서 HWPX 원문, 검증 데이터, 조직 매핑을 결합하고, 실행에 필요한 사업 데이터를 HTML 내부 JavaScript 상수로 삽입합니다.

```text
과기정통부 HWPX 1~8권
        │
        ▼
HWPX 본문 텍스트 추출(work/hwpx_text)
        │
        ├─ 사업시행주체 검증
        ├─ 세부사업·내역사업 및 예산 추출
        ├─ 사업목적·내용 문장 추출
        └─ 소관 과·팀 추출
        │
        ▼
NIPA 사업 정규화 및 분류
        │
        ├─ 모사업/내역 전용 카드 결정
        ├─ 내역별 설명·추진방향 생성
        ├─ 예산 레이어 분류
        ├─ 과기정통부 조직 매핑
        └─ NIPA 담당본부 매핑
        │
        ▼
검증 및 합계 대사
        │
        ├─ 사업 수·예산 합계
        ├─ 레이어 합계
        ├─ 조직 합계
        └─ 제외 대상 확인
        │
        ▼
index.html / JSON 산출물
```

## 2. 생성 단계

### 입력 계층

- `ai-budget-ecosystem-dashboard.html`: 공통 UI와 기본 데이터 구조를 제공하는 기준 템플릿
- `work/hwpx_text/`: HWPX 압축문서에서 추출한 본문 텍스트
- `all-nipa-budget-block-audit.json`: 사업 블록과 시행주체 전수 점검 결과
- `nipa-execution-mapping-audit.json`: 공동 시행 및 NIPA 귀속 검증 결과
- `NIPA_사업별_담당본부_재검증DB.xlsx`: NIPA 담당본부와 내역별 예산 매핑 입력

입력 계층에는 원문과 내부 검증자료가 포함되므로 Git 저장소에서는 제외합니다.

### 변환 계층

핵심 생성기는 `build-nipa-ai-budget-dashboard.mjs`입니다.

1. 사업시행주체에 NIPA가 명시된 사업만 선별합니다.
2. 공동 시행 사업은 NIPA 담당 내역과 금액만 분리합니다.
3. `사업목적·내용` 영역에서 사업 또는 내역명과 직접 연결된 설명을 추출합니다.
4. 내역 전용 카드에는 다른 내역이나 모사업의 일반 설명이 섞이지 않도록 일치도가 높은 문장만 사용합니다.
5. 정책목적을 기준으로 9개 예산 레이어에 매핑합니다.
6. 과기정통부 소관 과·팀이 내역별로 다르면 조직 화면에서 카드를 분리합니다.
7. NIPA 담당본부가 내역별로 다르면 본부 화면에서 예산과 카드를 분리합니다.
8. 레이어·조직·본부별 합계와 전체 합계를 대사합니다.

### 출력 계층

- `index.html`: GitHub Pages용 진입 파일
- `nipa-ai-budget-ecosystem-dashboard.html`: 동일 내용의 명시적 결과 파일
- `nipa-ai-budget-projects.json`: 정규화된 사업 데이터와 합계
- `nipa-budget-verification.json`: 포함·제외·합계 검증 결과

## 3. 브라우저 실행 구조

HTML 내부에는 다음 데이터 집합이 포함됩니다.

- `PROJECTS`: 기본 사업 카드
- `ORG_PROJECTS`: 과기정통부 소관별 내역 분리 카드
- `NIPA_HQ_PROJECTS`: NIPA 본부별 내역 분리 카드
- `LAYERS`: 레이어별 사업 수와 예산 합계
- `ORGS`, `ORG_TREE`: 과기정통부 조직 트리와 집계
- `NIPA_HEADQUARTERS`: NIPA 본부별 집계

화면의 검색·필터·정렬·팝업은 모두 브라우저 JavaScript로 처리합니다. 외부 API 호출이나 서버 데이터베이스가 없기 때문에 파일 하나만 배포해도 동작합니다.

## 4. 카드 구성 원칙

- 모사업 카드: 모사업 목적과 전체 내역표 표시
- 내역 전용 카드: 해당 내역의 목적·내용만 표시하고 모사업 전체 내역표는 유지
- 조직 분리 카드: 해당 소관 과·팀이 담당하는 내역의 설명과 예산 표시
- 본부 분리 카드: 해당 NIPA 본부가 담당하는 내역의 설명과 예산 표시

사업 상세 상단은 `레이어 → 사업코드/회계 → 소관 본부 → 예산` 순서로 구성됩니다.

## 5. 레이어 분류 원칙

단어의 존재보다 사업이 해결하려는 최종 정책목적을 우선합니다.

- AI인프라: 컴퓨팅·GPU·데이터센터·공동 활용 기반
- AI반도체: NPU·온디바이스·국산 AI반도체 실증과 수요창출
- AI모델: 파운데이션 모델·에이전트·AI 응용모델 개발과 확산
- 지역AX: 지역 산업·기업·인재·균형발전이 직접 목적
- 피지컬AI: 현실공간 인식·로봇·자율제어·LAM·공간컴퓨팅
- 글로벌 이니셔티브: 해외실증·수출·국제협력·글로벌 의제
- AI생태계: 인재·창업·오픈소스·규제·표준·산학연 협력
- 산업전략: 의료·제조·콘텐츠·물류 등 특정 산업의 개발·실증·사업화
- 기관운영비: NIPA 지원 및 SW정책연구소 운영

## 6. 변경 및 검증 절차

1. 원문 또는 매핑 규칙을 수정합니다.
2. `npm run build`로 산출물을 다시 생성합니다.
3. `nipa-budget-verification.json`에서 조직·레이어 합계 일치 여부를 확인합니다.
4. `index.html`을 브라우저에서 열어 검색, 탭, 팝업, 내역표를 점검합니다.
5. 생성 스크립트와 결과 파일을 함께 커밋합니다.

